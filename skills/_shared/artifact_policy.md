# Artifact Policy

Generated context bundles, plans, configs, and run scripts belong under ignored `artifacts/agent_context/`, `artifacts/agent_plans/`, `runs/generated/`, or `plans/generated/` paths unless the user explicitly asks otherwise.
