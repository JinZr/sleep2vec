name: sleep2vec2_checkpoint_inference_example
task: infer
variant: sleep2vec2
experiment:
  id: ASK_USER
  title: ASK_USER
  objective: ASK_USER
  root: ASK_USER
  baseline: ASK_USER
step:
  id: evaluate-sleep2vec2-checkpoint
  phase: evaluate
  purpose: Evaluate an explicitly selected sleep2vec2 checkpoint.
inputs:
  config: configs/sleep2vec2/ppg_ahi_finetune_large.yaml
  label_name: ahi
  ckpt_path: ASK_USER
