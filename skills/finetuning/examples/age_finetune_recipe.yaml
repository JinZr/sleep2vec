name: age_finetune_example
task: finetune
variant: sleep2vec
experiment:
  id: ASK_USER
  title: ASK_USER
  objective: ASK_USER
  root: ASK_USER
  baseline: ASK_USER
step:
  id: train-age
  phase: train
  purpose: Finetune the age task.
inputs:
  config: configs/examples/age/FINETUNE_EXAMPLE.yaml
  label_name: age
