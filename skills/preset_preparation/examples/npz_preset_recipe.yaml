name: npz_preset_example
task: preset_prepare
variant: sleep2vec
experiment:
  id: ASK_USER
  title: ASK_USER
  objective: ASK_USER
  root: ASK_USER
  baseline: ASK_USER
step:
  id: prepare-npz-preset
  phase: prepare
  purpose: Prepare the NPZ preset.
inputs:
  config: configs/ppg_ahi_finetune_large.yaml
  index: [index/example.csv]
  dataset_name: example
