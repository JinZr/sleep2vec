# Changelog

## 2026-04-21

- Initialized the `fix/investigate-slow-pretrain-presetsx` branch handbook from the checked-out branch state.
- Refreshed the preset-generation and sample-validation function pages for the current worker contract: omitting `--num-workers` restores automatic validation-thread selection for a single preset build, while multi-job builds still run one validation thread per worker process by default.
- Recorded the current branch metadata, tracked-file counts, and working-tree-only delta relative to `main`.
