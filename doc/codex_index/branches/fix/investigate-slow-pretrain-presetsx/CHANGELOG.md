# Changelog

## 2026-04-21

- Initialized the `fix/investigate-slow-pretrain-presetsx` branch handbook from the checked-out branch state.
- Refreshed the preset-generation and sample-validation function pages for the current worker contract: omitting `--num-workers` restores automatic validation-thread selection, while explicit `--num-workers` is now forwarded unchanged to each multi-job inner validation path.
- Recorded the current branch metadata, tracked-file counts, and working-tree-only delta relative to `main`.
