# Agent Contracts

This directory defines contracts for experiment workspaces, agent context bundles, task recipes, run manifests, user decisions, and external-test locking.

New runnable plans must follow `experiment_workspace.md`.
